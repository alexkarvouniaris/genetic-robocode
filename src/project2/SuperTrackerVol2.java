package project2;

import robocode.AdvancedRobot;

public class SuperTrackerVol2 extends AdvancedRobot {
	/*Here goes the updated SuperTracker's code that implements the genetic algorithm.
	 * 1. Reads the set of the 4 genes(configuration objects) from the disk file
	 * 2. Creates a population and evolves it
	 */
}
